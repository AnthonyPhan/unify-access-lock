{
  "name": "unifi-access-lock",
  "version": "0.1.0",
  "private": true,
  "description": "DPS-aware UniFi Access relocking with a local Raspberry Pi configuration UI.",
  "type": "module",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "check": "node --check src/*.js && node --check public/app.js && node --test"
  }
}
